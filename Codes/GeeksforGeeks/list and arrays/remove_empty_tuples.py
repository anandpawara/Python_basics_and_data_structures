
tuples = [(), ('ram','15','8'), (), ('laxman', 'sita'),  
          ('krishna', 'akbar', '45'), ('',''),()] 

# Using list comprehensions
# new_list = [t for t in tuples if t]
# print(new_list)

# Using filter and lambda expression
# In filter method the element is check with the function ,if the element is not None then element is added to list
# new_list = list(filter(None ,tuples))
# print(new_list)